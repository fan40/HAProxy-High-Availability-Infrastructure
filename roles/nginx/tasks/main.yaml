---
# 1️⃣ Mise à jour du cache apt
- name: Update apt cache
  apt:
    update_cache: yes
# Installation nginx
- name: Installer Nginx
  apt:
    name: nginx
    state: present
# activation du service nginx
- name: Activer Nginx
  service:
    name: nginx
    state: started
    enabled: yes
